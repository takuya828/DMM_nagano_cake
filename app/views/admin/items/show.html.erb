<%= flash[:danger] %>
<%= flash[:success] %>

<main>
  <div class="container">
      <div class="mt-3">
      <h4>商品詳細</h4>
      </div>
   <div class="row">
    <div class="mt-5 mx-3">
<div class="col">
<%= attachment_image_tag(@item, :image, :fill, 300, 200, format: 'jpeg', fallback: "no_image.jpg") %>
</div>
</div>

<div class="mt-5">
<div class="col">
<p>商品名　　　　　　　　　　<strong><%= @item.name %></strong></p>
<p>商品説明　　　　　　　　　<strong><%= @item.introduction %></strong></p>
<p>ジャンル　　　　　　　　　<strong><%= @item.genre.name %></strong></p>
<p>税込価格（税抜価格）　　　<strong><%= @item.add_tax_price.to_s(:dalimited)%>（<%= @item.price.to_s(:dalimited) %>）</strong>円</p>
</div>
</div>
</div>
<div class="text-center">
<%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
</div>
</div>