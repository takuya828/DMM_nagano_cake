
<div class="container">
  <div class="mx-3">

  <div class="row">
    <div class="col">
<div class="mt-3">
<h4>商品一覧</h4>
</div>
<div class="text-right"><%= link_to "商品新規登録", new_admin_item_path %></div>

<% @items.each do |item| %>

  <p>商品ID   <%= item.id %></p>
  <p>商品名　 <%= link_to item.name, admin_item_path(item) %></p>
  <p>税抜価格   <%= item.price %></p>
  <p>ジャンル   <%= item.genre.name %></p><br><br>
<% end %>
</div>
</div>
</div>
</div>
