<h1>注文履歴詳細</h1>
<p>購入者　　　<%= link_to (@order.customer.first_name + @order.customer.last_name), admin_customer_path(@order.customer) %>
<p>注文日　　　<%= @order.created_at.strftime("%Y-%m-%d%H:%M") %></p>
<p>配送先　　 〒<%= @order.postal_code %><%= @order.address %><br>　　　　　　<%= @order.name %></p>
<p>支払方法　　<%= @order.payment_type_i18n %></p>

<% @order_items.each do |order_item| %>
<p>商品名<%= order_item.item.name %></p>
<p>単価(税込) <%= (order_item.item.price * 1.1).floor %></p>
<p>数量 <%= order_item.amount %></p>
<p>小計 <%= (order_item.item.price * 1.1).floor * order_item.amount %></p>
<% end %><br><br>

<h3>商品合計 <%= @order.price %>円</h3>
<h3>送料 <%= @postage %>円</h3>
<h3>請求金額合計 <%= (@order.price + @postage).to_s(:delimited) %>円</h3>