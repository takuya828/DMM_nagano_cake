<div class="container">
  <div class="mt-3 mx-3">
<h1>ジャンル編集</h1>

    <div class="row">
      <div class="col">
        <div class="mt-3">

<%= form_with model: [:admin, @genre], local:true do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>　　　　　 <%= f.submit '変更を保存', class:"btn btn-success" %>
    <% end %>

    </div>
    </div>
    </div>